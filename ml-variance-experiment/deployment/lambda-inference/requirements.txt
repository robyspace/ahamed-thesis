# AWS Lambda Inference Requirements
# These packages will be bundled with the Lambda deployment package

# Core ML libraries
scikit-learn==1.3.2
numpy==1.24.3
joblib==1.3.2

# Note: For AWS Lambda, you may need to use pre-built layers
# or build these packages on Amazon Linux for compatibility
